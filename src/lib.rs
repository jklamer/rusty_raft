mod protocol;




pub async fn raft_push_off() -> Result<(), Box<dyn std::error::Error + Send + Sync>> {
    


    

    Ok(())
} 


#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
